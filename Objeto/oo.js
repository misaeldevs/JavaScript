//CÓDIGO NÃO EXECUTÁVEL!!

//Procedural -> Baseado no procedimento(procedure), na função, no qual se nomeia um trecho de códigos podendo reutilizá-lo. Funções manipulam dados.

processamento(valor1, valor2, valor3)

//Orientação à Objetos -> Dados que contêm funções. O Objeto é a figura central. Uma estrutura que gera objetos.
objeto = {
    valor1,
    valor2,
    valor3,
    processamento(){

    }
}
objeto.processamento() // Foco passou a ser o Objeto.

// 1. Abstração;
// 2. Encapsulamento;
// 3. Herança (prototype);
// Polimorfismo.